package org.section2.datatypes_var_n_literals;

public class DataTypes {
    public static void main(String[] args) {
//in java first we have to declare data then we can then we can use the data
        //whenever we need variable we have to select any data type from pre-defined data type and declare varible of that type
        // compiler given datatype called primitive datatypes
        //types: 1.integral(byte ,short ,int , long), 2.floating point(float, double), 3.char ,4.boolean
// java support unicode , AsCII is subset of unicode
        // for boolean , size is not mention it depends on jvm

        //  class availbe for int data type is Integer, this Integer class is wrappered around datatype so this wrapper class
        // this class have infor of range of datatype ,and size

        System.out.println("Int min value : "+Integer.MIN_VALUE);
        System.out.println("Int Max value : "+Integer.MAX_VALUE);

    }


}
